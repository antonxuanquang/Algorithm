
public class Three {
	
	/*
	 * 
	 * Magic Index: A magix index in an array A[0...n-1] is defined to be
	 * an index such that A[i] = i. Given a sorted array of distinct integers,
	 * write a method to find a magic index, if one exists, in array A.
	 * 
	 * FOLLOW UP: What if the values are not distinct?
	 * 
	 */
	
	// O(n) algorithm
	public static int magicIndex(int[] array) {
		int index = 0;
		int value;
		while (index < array.length) {
			if (index == array[index]) return index;
			index = array[index];
		}
		return -1;
	}
	
	public static void main (String[]args) {
		int[] array = {1, 2, 3, 4, 4, 6, 7, 10};
		System.out.println(magicIndex(array));
	}

}
