
public class One {
	
	/*
	 * 
	 * Triple Step: A child is running up a staircase with n steps
	 * and can hop either 1 step, 2 steps, or 3 steps at a time. 
	 * Implement a method to count how many possible ways the child
	 * can run up the stairs
	 * 
	 */
	
	public static int tripleStep(int n) {
		return tripleStep(n, new int[n + 1]);
	}

	private static int tripleStep(int n, int[] memmo) {
		if (n == 1) return 1;
		if (memmo[n] == 0) {
			memmo[n] = tripleStep(n - 1, memmo) + tripleStep(n - 2, memmo)
					+ tripleStep(n - 3, memmo);
		}
		return memmo[n];
	}

}
